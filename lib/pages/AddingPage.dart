import 'package:flutter/material.dart';

class addingPage extends StatelessWidget {
  const addingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Adding task page ", style: TextStyle(fontSize: 25)),
      ),
    );
  }
}
